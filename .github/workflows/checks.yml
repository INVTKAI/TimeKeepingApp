name: checks

on:
  pull_request:
  push:
    branches: [main]

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  fast-checks:
    name: Fast checks
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4

      - name: Service-role-key usage lint (§11.6 P0 gate)
        run: bash backend/scripts/lint-service-role-usage.sh

      - uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: frontend/package-lock.json

      - name: Install frontend deps
        working-directory: frontend
        run: npm ci

      - name: Frontend typecheck + build
        working-directory: frontend
        run: npm run build

  pgtap:
    name: pgTAP (DB invariants, 241 assertions)
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v4

      - uses: supabase/setup-cli@v1
        with:
          version: latest

      - name: Start Supabase local stack
        working-directory: backend
        run: supabase start

      - name: Run pgTAP suite
        working-directory: backend
        run: supabase test db

      - name: Stop Supabase
        if: always()
        working-directory: backend
        run: supabase stop

  # EF integration tests are the slowest layer (~10 min including boot) and
  # depend on a populated backend/.env. Wire them on `main` only once the CI
  # secrets story is settled — see docs/pre-deploy-uat-spec.md Layer 1.
  #
  # ef-integration:
  #   name: Edge Function integration tests (44 tests across 10 EFs)
  #   if: github.event_name == 'push' && github.ref == 'refs/heads/main'
  #   runs-on: ubuntu-latest
  #   timeout-minutes: 30
  #   steps:
  #     - uses: actions/checkout@v4
  #     - uses: denoland/setup-deno@v1
  #       with: { deno-version: v1.x }
  #     - uses: supabase/setup-cli@v1
  #       with: { version: latest }
  #     - name: Create backend/.env from CI secrets
  #       run: |
  #         printf 'NOTIFICATION_DRAIN_SECRET=%s\n' "${{ secrets.CI_DRAIN_SECRET }}" > backend/.env
  #     - name: Start Supabase local stack
  #       working-directory: backend
  #       run: supabase start
  #     - name: Run EF integration tests
  #       working-directory: backend
  #       run: bash scripts/run-ef-tests.sh
  #     - name: Stop Supabase
  #       if: always()
  #       working-directory: backend
  #       run: supabase stop
